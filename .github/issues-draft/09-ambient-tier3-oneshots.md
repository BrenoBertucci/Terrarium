## Context

Tier 3 from the ambient expansion brief — only if Tier 2 is done or there
is spare capacity. One-shots only (Thunder pattern), never discrete cricket
schedulers.

| key | notes |
| --- | --- |
| `door_open` / `door_close` | if engine does not already own door SFX |
| `wind_gust` | short; tied to `Wind.gust()` peaks |
| `distant_thunder` | complement to existing `thunder.ogg` |

Confirm footsteps are already engine-owned before adding any step work.

CC0 1.0 only. Same CREDITS discipline as Tier 1.
